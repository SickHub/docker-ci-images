name: Build and Push DinD Images to GHCR

on:
  push:
#    branches:
#      - main
#  pull_request:
#    branches:
#      - main

jobs:
  build-and-push-image:
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/dind-images.yml
    with:
      version: "latest"
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}