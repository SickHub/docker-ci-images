{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "enabledManagers": [
    "custom.regex"
  ],
  "packageRules": [
    {
      "matchDatasources": ["github-releases"],
      "matchPackageNames": ["kubernetes-sigs/kustomize"],
      "versionCompatibility": "^(?<compatibility>.*)/(?<version>.*)$"
    },
    {
      "matchUpdateTypes": ["patch", "pin", "digest"],
      "automerge": true
    },
    {
      "matchDepTypes": ["devDependencies"],
      "automerge": true
    }
  ],
  "platformAutomerge": true,
  "customManagers": [
    {
      "customType": "regex",
      "fileMatch": [
        "^.circleci/config.yml$",
        "^images.*/.*/Dockerfile$"
      ],
      "matchStrings": [
        "\\sdefault: (?<currentValue>.*?) # (?<datasource>.*?):(?<depName>.*?)\\n",
        "\\sdefault: (?<currentValue>.*?) # (?<datasource>.*?):(?<depName>[^\\s:]*?):(?<extractVersion>.*?)\\n",
        "# (?<datasource>.*?):(?<depName>.*?)\\sARG .*?_VERSION=(?<currentValue>.*)\\n",
        "# (?<datasource>.*?):(?<depName>[^\\s:]*?):(?<extractVersion>.*?)\\sARG .*?_VERSION=(?<currentValue>.*)\\n"
      ]
    }
  ]
}
